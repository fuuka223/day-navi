<h1>タスク一覧</h1>

<%= link_to "タスク追加", new_task_path %>

<div>
  <% @tasks.each do |task| %>
    <%= link_to task_path(task) do %>
      <div>
        <h3><%= task.title %>(優先度: <%= task.priority_level %>)</h3>
        <p><%= task.content %></p>
      </div>
    <% end %>
  <% end %>
</div>